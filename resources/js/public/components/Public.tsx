import React from "react";
import { PublicRouter } from "../PublicRouter";

interface Props {}

export const Public = (props: Props) => {
    return <PublicRouter></PublicRouter>;
};

export default Public;
